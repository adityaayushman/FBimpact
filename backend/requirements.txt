# Backend deployment (Render). CPU-only torch: the default wheel pulls ~2.5 GB of
# CUDA libraries that a free-tier container has neither the disk nor the GPU for.
--extra-index-url https://download.pytorch.org/whl/cpu

torch==2.6.0+cpu
numpy>=1.24,<2
PyYAML>=6.0
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
